create table trade (
    filling_date date not null,
    starting_date date not null,
    price numeric,
    quantity numeric,
    insider_id numeric,
    tradeType_id numeric
);

create table trade (
    name text,
    title text,
    shares_owned,
    as_of_date
    company_id
);

create table trade (
    ticker
    cik
    industry_id
    lastUpdated
    lastFiling
);

create table trade (
    name
    sector
    subSector
);

create table trade (
    code
    type
);
